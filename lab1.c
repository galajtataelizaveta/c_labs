#include <stdio.h>
#include <math.h>

int main()

{
     int a, b, x, c, k;
     printf("Enter a -> ");
     scanf("%d", &a);

     printf("Enter b -> ");
     scanf("%d", &b);

     printf("Enter x -> ");
     scanf("%d", &c);
     if(k > c)
         
        if (a < b)
            printf(c = a + x)
        else:
            printf(k = a - x)
     printf (k)
     else: 
         printf (c)

    return 0;
}
